"""The headline result: effect-size vs schema-nameability MAP (implementation.md §6, Phase 6). STUB.

Order DBs/tasks by a nameability proxy (header informativeness / coded-column fraction / language); plot
Delta(full - name_only) and name-shuffle survival across them + the FK-role qualitative win. The main figure.
"""
from __future__ import annotations


def build_gradient_map(*args, **kwargs):
    raise NotImplementedError("Phase 6 — after GATE 2 (the audit).")
