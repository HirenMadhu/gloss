"""HALOS-minimal: RT-style relational transformer (implementation.md §6). Phase-3 STUB.

Geometry as substrate: relational attention + FiLM doc fusion + FK-role edges + SIMPLE temporal. <=~30M
params. FK-role invariant (Phase-3 test): swapping fk_role_id changes predictions and name_only cannot
distinguish two FKs into one table. Build only after GATE 1.
"""
from __future__ import annotations


class HALOSMinimal:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError("Phase 3 — build after GATE 1 (the proxy gate).")
