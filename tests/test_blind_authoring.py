"""Blind cards don't leak labels (impl §9): placebo stays ~0; semantic signal survives blind authoring.
Load-bearing given Claude-authored docs (plan stress-test #1). Phase 5.
"""
import pytest

pytest.skip("Phase 5 (GATE 2): audit/controls.py not built yet.", allow_module_level=True)
