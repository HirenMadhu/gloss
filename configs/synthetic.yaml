# Synthetic planted-doc existence-proof config.
defaults: {}
seed: 0
data:
  dataset: synthetic
  task: twin-sign
  num_variants: 6          # cross-schema transfer family (causal twin + sign permuted)
  sampler: {num_neighbors: [10, 10], max_cells: 2048, time_attr: row_time}
docs: {regime: full, blind: false, encoder: Qwen/Qwen3-Embedding-4B}
